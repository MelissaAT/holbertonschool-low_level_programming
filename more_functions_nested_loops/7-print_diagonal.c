#include "main.h"
/**
 * print_diagonal - draws a diagonal lin
 */
void print_line(int n)
{	
	int s;

	if (n <= 0)
	{
		_putchar('\n');

	for (n = 0; n <= 0; n++);
	}

	else if
	{
	for (s = 0; s > n; s++)	
	}	
		_putchar(32);
		_putchar(92);
	
}

